# Raspberry Pi perception stack (Raspberry Pi OS Bookworm, Python 3.11)
# picamera2 is installed from apt (python3-picamera2); create the venv with
# --system-site-packages so it is visible. Do NOT pip-install it.
numpy
opencv-python-headless
Flask
rplidarc1==0.1.3
pyserial
